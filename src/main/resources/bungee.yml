name: SendPlayers
main: de.manacity.sendall.SendAll
version: 1.0
author: TabZz147
commands:
  sendplayers:
    description: Verschiebt alle Spieler von einem Server auf einen anderen
    usage: /sendplayers <fromServer> <toServer>
    aliases: [sp, sendp]
    permission: manacity.sendall
permissions:
  manacity.sendall:
    description: Erlaubt Zugriff auf den SendPlayers-Befehl
    default: false
